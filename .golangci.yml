version: "2"
linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - exhaustive
    - gocritic
    - gosec
    - misspell
formatters:
  enable:
    - gofmt
    - goimports
run:
  timeout: 5m
  go: "1.24"
